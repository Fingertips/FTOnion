Pod::Spec.new do |s|
  s.name     = 'FTOnion'
  s.version  = '0.1'
  s.license  = 'MIT'
  s.author   = { 'Eloy Duran' => 'eloy.de.enige@gmail.com' }
  s.summary  = 'A set of Objective-C helpers that query an iOS/OS X UI.'
  s.homepage = 'https://github.com/Fingertips/FTOnion'
  s.source   = { :git => 'git://github.com/Fingertips/FTOnion.git', :tag => '0.1' }

  s.source_files = 'Classes'
end
